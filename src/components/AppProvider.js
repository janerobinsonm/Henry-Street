import React from "react";
// import { MapViewProvider } from "./map/MapViewProvider"

export default (props) => {
  return (
    <>
    {/* <MapViewProvider> */}
      {props.children}
    {/* </MapViewProvider> */}
    </>
  );
};
